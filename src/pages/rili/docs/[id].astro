---
import BaseLayout from "@/layouts/BaseLayout.astro";
import RiliDoc from "@/components/Rili/RiliDoc";

const { document } = Astro.params;
---

<BaseLayout
  title={`Rili document ${document}`}
  description={`This rili document is about ${document}`}
>
  <RiliDoc client:only="react" id={document} />
</BaseLayout>
